<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.genuine.hm.repository.user.UserAccountRepository">
   <select id="findOne" resultType="jp.co.genuine.hm.model.user.UserAccount">
      SELECT id, user_id AS userId, password FROM user_account WHERE user_id = #{userId};
   </select>
   <insert id="register">
      INSERT INTO user_account (id, user_id, password) VALUES(nextval('user_account_id_seq'), #{userId}, #{password} );
   </insert>
</mapper>